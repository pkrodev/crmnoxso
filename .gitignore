# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Konfiguracja lokalna — NIGDY nie commitujemy sekretów
.env
.env.local

# Dane osobowe klientów — arkusze wejściowe NIE trafiają do repozytorium.
# Plik produkcyjny zawiera 1924 rekordy z nazwiskami, telefonami, NIP-ami
# i adresami. Trzymaj go lokalnie i wgrywaj przez ekran /import.
*.xlsx
*.xls
*.ods
*.csv

# Paczki pobrane ręcznie (obejście blokady PyPI w sieci firmowej)
wheels/*.whl
wheels/*.tar.gz

# Tailwind standalone CLI — binarka, pobierana osobno
tools/

# Wynik builda CSS — CELOWO commitowany.
# Railway nie ma Node'a ani standalone CLI Tailwinda, a Procfile w kroku
# `release` odpala wyłącznie migracje. Gdyby ten plik był ignorowany,
# wdrożona aplikacja poszłaby na produkcję bez arkusza stylów.
# Po zmianach w szablonach: .\scripts\build_css.ps1 i commit wyniku.

# Pliki wgrane przez użytkownika na /import
instance/
uploads/

# Zrzuty rozmów z Claude Code zapisane w katalogu projektu
20??-??-??-*.txt

# Windows / edytory
Thumbs.db
desktop.ini
.vscode/
.idea/
